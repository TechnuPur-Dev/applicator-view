# Set the registry to the default npm registry
registry=https://registry.npmjs.org/

# # Disable package-lock.json generation (useful for libraries)
# package-lock=false

# # Specify where node_modules will be installed (optional)
# prefix=./.npm-global

# # Set log level to warn (less verbose)
# loglevel=warn

# # Use a custom cache directory (optional)
# cache=./.npm-cache

# # Set authentication token for private registry (if needed)
# # //registry.npmjs.org/:_authToken=your-auth-token

# # Allow scripts to run during install (set to false to disable)
# ignore-scripts=false
